name: Windows Release

concurrency:
  group: windows-release-${{ github.ref }}
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      version:
        description: Dry-run version matching pom.xml
        required: true
        default: 0.3.0
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  dry-run:
    if: github.event_name == 'workflow_dispatch'
    runs-on: windows-latest
    steps:
      # 外部 Action：取出目前 commit；完整 SHA 對應 actions/checkout v6.0.2。
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      # 外部 Action：安裝 Java 25；完整 SHA 對應 actions/setup-java v5.6.0。
      - name: Set up JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
          check-latest: false

      # 外部工具：建立未簽章、未公開的完整 Setup，供維護者驗收。
      - name: Build dry-run Setup
        shell: pwsh
        run: powershell.exe -NoProfile -File scripts\build-windows-installer.ps1 -Version "${{ inputs.version }}"

      # 外部工具：驗證版本、SBOM、notices、Setup 與非商用簽章狀態。
      - name: Verify dry-run Setup
        shell: pwsh
        run: powershell.exe -NoProfile -File scripts\verify-release.ps1 -Version "${{ inputs.version }}" -InstallerPath "dist\LinebotDocument-Setup-${{ inputs.version }}.exe"

      # 外部 Action：保存未公開診斷 artifact；完整 SHA 對應 actions/upload-artifact v7.0.1。
      - name: Upload dry-run artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: windows-setup-dry-run-${{ inputs.version }}
          path: dist/LinebotDocument-Setup-${{ inputs.version }}.exe
          if-no-files-found: error
          retention-days: 7

  release:
    if: github.event_name == 'push'
    runs-on: windows-latest
    environment: commercial-release
    permissions:
      contents: write
    steps:
      # 外部 Action：以 Release job 專用憑證取出 Tag；完整 SHA 對應 actions/checkout v6.0.2。
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      # 外部 Action：安裝 Java 25；完整 SHA 對應 actions/setup-java v5.6.0。
      - name: Set up JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
          check-latest: false

      # 演算法步驟一：由受限制格式的 Tag 取得唯一版本號。
      - name: Resolve version
        id: version
        shell: pwsh
        run: |
          $Version = "${{ github.ref_name }}".Substring(1)
          "value=$Version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      # 外部工具：完整測試、產生 SBOM、app image 與單一 Setup。
      - name: Build release Setup
        shell: pwsh
        run: powershell.exe -NoProfile -File scripts\build-windows-installer.ps1 -Version "${{ steps.version.outputs.value }}"

      # 演算法步驟二：偵測是否具備簽章能力，決定走商用簽章或未簽章個人發佈。
      - name: Detect signing capability
        id: signing
        shell: pwsh
        env:
          SIGNING_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE_BASE64 }}
        run: |
          $Available = -not [string]::IsNullOrWhiteSpace($env:SIGNING_CERTIFICATE_BASE64)
          "available=$($Available.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      # 發佈閘門：法律／品牌 placeholder 必須在接觸簽章秘密前先失敗。
      - name: Verify commercial metadata
        if: steps.signing.outputs.available == 'true'
        shell: pwsh
        run: powershell.exe -NoProfile -File scripts\verify-release.ps1 -Version "${{ steps.version.outputs.value }}" -Tag "${{ github.ref_name }}" -InstallerPath "dist\LinebotDocument-Setup-${{ steps.version.outputs.value }}.exe" -RequireCommercialMetadata

      # 外部工具：使用受保護 Environment PFX 簽署 Setup，secret 不寫入檔案或 Log。
      - name: Sign Setup
        if: steps.signing.outputs.available == 'true'
        shell: pwsh
        env:
          SIGNING_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE_BASE64 }}
          SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE_PASSWORD }}
        run: powershell.exe -NoProfile -File scripts\sign-windows-artifacts.ps1 -ArtifactPath "dist\LinebotDocument-Setup-${{ steps.version.outputs.value }}.exe"

      # 外部工具：簽章後重新驗證版本、商用欄位、SBOM 與 Authenticode，並取得 SHA-256。
      - name: Verify signed Setup
        if: steps.signing.outputs.available == 'true'
        shell: pwsh
        run: |
          $Output = powershell.exe -NoProfile -File scripts\verify-release.ps1 -Version "${{ steps.version.outputs.value }}" -Tag "${{ github.ref_name }}" -InstallerPath "dist\LinebotDocument-Setup-${{ steps.version.outputs.value }}.exe" -RequireCommercialMetadata -RequireSignature
          $Hash = ($Output | Where-Object { $_ -like "SHA256=*" }).Substring(7)
          "SETUP_SHA256=$Hash" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      # 外部工具：未設定簽章憑證時仍驗證版本、SBOM 與 notices，但不套用商用與簽章閘門。
      - name: Verify unsigned Setup
        if: steps.signing.outputs.available != 'true'
        shell: pwsh
        run: |
          $Output = powershell.exe -NoProfile -File scripts\verify-release.ps1 -Version "${{ steps.version.outputs.value }}" -Tag "${{ github.ref_name }}" -InstallerPath "dist\LinebotDocument-Setup-${{ steps.version.outputs.value }}.exe"
          $Hash = ($Output | Where-Object { $_ -like "SHA256=*" }).Substring(7)
          "SETUP_SHA256=$Hash" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      # 外部工具：驗證通過後才建立 GitHub Release，資產僅有一份 Setup.exe。
      # 未簽章版本一律標記為 pre-release，並在 Notes 說明 SmartScreen 行為，
      # 避免未簽章檔案被當成正式商用版散布。
      - name: Publish GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          SIGNED: ${{ steps.signing.outputs.available }}
        run: |
          $Version = "${{ steps.version.outputs.value }}"
          $Tag = "${{ github.ref_name }}"
          $Asset = "dist\LinebotDocument-Setup-$Version.exe"

          if ($env:SIGNED -eq "true") {
            $Lines = @("SHA-256: $env:SETUP_SHA256")
          }
          else {
            $Lines = @(
              "未簽章的個人使用版本，尚未取得 Windows 程式碼簽章憑證。",
              "",
              "首次執行會出現 SmartScreen 藍色警告，點「其他資訊」再點「仍要執行」即可。",
              "安裝不需要系統管理員權限，只安裝給目前的 Windows 使用者。",
              "",
              "SHA-256: $env:SETUP_SHA256"
            )
          }

          $Notes = $Lines -join "`n"
          $Title = "Linebot Document $Version"

          if ($env:SIGNED -eq "true") {
            gh release create $Tag $Asset --draft --verify-tag --title $Title --notes $Notes
          }
          else {
            gh release create $Tag $Asset --draft --verify-tag --prerelease --title $Title --notes $Notes
          }

          if ($LASTEXITCODE -ne 0) { throw "建立 GitHub Release 草稿失敗，exit code：$LASTEXITCODE" }

          gh release edit $Tag --draft=false
