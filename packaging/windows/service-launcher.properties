# 背景服務 launcher 固定使用獨立 service mode，不建立 Swing 或系統匣。
arguments=--app.runtime.mode=service

# JDK 25 允許 SQLite JDBC 載入原生程式庫，並停用服務程序不需要的 AWT。
java-options=--enable-native-access=ALL-UNNAMED -Djava.awt.headless=true -Dapp.runtime.mode=service
